
module Interpret.Foreign where

-- ctype + modulus type -> val/function
-- foreign-alias context "nuklear.h nk_context"

-- foreign-import init "nukler.h nk_init_default" 

-- modulus type -> ctype


-- foreignAlias :: Expr -> String -> EvalM (Ctx -> Ctx)

-- foreignImport :: String -> String -> Expr -> EvalM (Ctx -> Ctx)
