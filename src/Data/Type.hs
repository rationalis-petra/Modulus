module Data.Type where

-- data PrimType = BoolT | CharT | IntT | FloatT | UnitT | StringT 
--   deriving (Eq, Ord)


-- data ModulusType
--   = TypeN Int
--   | MVariable String
--   | Signature (Map.Map String ModulusType)
--   | MFunction ModulusType ModulusType
--   | Equiv Expr
--   | Singleton Expr

--   -- "Primitive" / native types
--   | MPrimitive PrimType
--   | MList ModulusType
--   | MVector ModulusType
--   | MVaraint Int

--   -- BAD -> we want him gone!
--   | Undef
